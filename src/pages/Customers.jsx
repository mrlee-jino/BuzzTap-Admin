import { useState } from "react"
import { useAdminData } from "../context/AdminDataContext"
import Modal from "../components/Modal"
import ConfirmModal from "../components/ConfirmModal"
import Toast from "../components/Toast"

const statuses = ["PENDING", "ACTIVE", "SUSPENDED", "FROZEN", "CLOSED", "DELETED"]
const inputClass = "mt-2 w-full rounded-xl border border-white/10 bg-[#080808] px-3 py-2.5 text-sm text-white outline-none focus:border-yellow-400"

function Customers() {
  const { customers, loadCustomer, updateCustomerStatus } = useAdminData()
  const [search, setSearch] = useState("")
  const [status, setStatus] = useState("All")
  const [selected, setSelected] = useState(null)
  const [loadForm, setLoadForm] = useState({ amount: "", reason: "" })
  const [confirm, setConfirm] = useState(null)
  const [toast, setToast] = useState(null)
  const filtered = customers.filter((customer) => (status === "All" || customer.status === status) && [customer.name, customer.email, customer.phone, customer.id, customer.cardId].join(" ").toLowerCase().includes(search.toLowerCase()))
  const askStatus = (customer, nextStatus) => setConfirm({ title: `${nextStatus === "DELETED" ? "Soft delete" : nextStatus} customer?`, description: `${customer.name} will be marked ${nextStatus}. This change is recorded in the mock audit log.`, label: nextStatus === "DELETED" ? "Soft Delete" : nextStatus, destructive: ["DELETED", "SUSPENDED", "FROZEN", "CLOSED"].includes(nextStatus), action: () => { updateCustomerStatus(customer.id, nextStatus); setConfirm(null); setToast({ message: `Customer marked ${nextStatus}.` }) } })
  const submitLoad = () => { const result = loadCustomer({ customerId: selected.id, amount: loadForm.amount, reason: loadForm.reason, reference: `CUS-${selected.id}`, notes: "Customer screen mock load" }); if (!result.ok) return setToast({ message: result.error, tone: "error" }); setSelected(null); setLoadForm({ amount: "", reason: "" }); setToast({ message: "BuzzPoints loaded." }) }
  const actions = (customer) => customer.status === "DELETED" ? ["ACTIVE"] : customer.status === "CLOSED" ? ["ACTIVE", "DELETED"] : ["SUSPENDED", "FROZEN", "CLOSED", "DELETED"]

  return <div className="space-y-8"><header><p className="text-sm font-bold tracking-[0.25em] text-yellow-400">MOCK CUSTOMER DIRECTORY</p><h1 className="mt-2 text-4xl font-bold">Customers</h1><p className="mt-2 text-gray-500">Manage customer identity, cards, balances, and lifecycle status.</p></header><div className="rounded-2xl border border-white/10 bg-[#111111] p-5"><div className="flex flex-col gap-3 lg:flex-row"><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Search name, email, phone, customer ID, NFC card ID" className="flex-1 rounded-xl border border-white/10 bg-[#080808] px-4 py-3 text-sm outline-none focus:border-yellow-400"/><select value={status} onChange={(event) => setStatus(event.target.value)} className="rounded-xl border border-white/10 bg-[#080808] px-3 py-2 text-sm"><option>All</option>{statuses.map((item) => <option key={item}>{item}</option>)}</select></div></div><div className="overflow-x-auto rounded-2xl border border-white/10 bg-[#111111]"><table className="w-full min-w-[900px] text-left text-sm"><thead className="border-b border-white/10 text-xs uppercase tracking-wider text-gray-600"><tr><th className="px-5 py-4">Customer</th><th>Contact</th><th>NFC Card</th><th>Balance</th><th>Status</th><th>Actions</th></tr></thead><tbody className="divide-y divide-white/5">{filtered.map((customer) => <tr key={customer.id}><td className="px-5 py-4"><p className="font-semibold">{customer.name}</p><p className="mt-1 font-mono text-xs text-gray-600">{customer.id}</p></td><td className="text-gray-400"><p>{customer.email}</p><p className="mt-1 text-xs text-gray-600">{customer.phone}</p></td><td className="font-mono text-gray-400">{customer.cardId}</td><td className="font-semibold text-yellow-400">{customer.balance.toLocaleString()} BP</td><td><span className="rounded-full bg-white/5 px-2 py-1 text-xs text-gray-300">{customer.status}</span></td><td className="py-3"><div className="flex flex-wrap gap-2"><button onClick={() => setToast({ message: `${customer.name} viewed.` })} className="text-xs text-gray-400 hover:text-white">View</button><button onClick={() => setToast({ message: "Edit is prototype-only." })} className="text-xs text-gray-400 hover:text-white">Edit</button><button onClick={() => { setSelected(customer); setLoadForm({ amount: "", reason: "" }) }} className="text-xs text-yellow-400">Load BP</button>{actions(customer).map((nextStatus) => <button key={nextStatus} onClick={() => askStatus(customer, nextStatus)} className="text-xs text-red-400">{nextStatus === "DELETED" ? "Soft Delete" : nextStatus === "ACTIVE" ? "Revive" : nextStatus}</button>)}</div></td></tr>)}</tbody></table>{filtered.length === 0 && <p className="py-12 text-center text-sm text-gray-500">No customers found.</p>}</div>{selected && <Modal title={`Load ${selected.name}`} onClose={() => setSelected(null)}><p className="text-sm text-gray-500">This mock load updates the shared treasury and customer balance.</p><label className="mt-5 block text-sm text-gray-400">Amount<input type="number" min="1" value={loadForm.amount} onChange={(event) => setLoadForm({ ...loadForm, amount: event.target.value })} className={inputClass}/></label><label className="mt-4 block text-sm text-gray-400">Reason<input value={loadForm.reason} onChange={(event) => setLoadForm({ ...loadForm, reason: event.target.value })} className={inputClass}/></label><div className="mt-6 flex justify-end gap-3"><button onClick={() => setSelected(null)} className="rounded-xl border border-white/10 px-4 py-2 text-sm text-gray-300">Cancel</button><button onClick={submitLoad} className="rounded-xl bg-yellow-400 px-4 py-2 text-sm font-semibold text-black">Confirm Load</button></div></Modal>}{confirm && <ConfirmModal {...confirm} onClose={() => setConfirm(null)}/>} {toast && <Toast {...toast} onClose={() => setToast(null)}/>}</div>
}

export default Customers
